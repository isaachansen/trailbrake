[package]
name = "overlay-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Sim-agnostic telemetry types, connector trait, reader loop, and mock source."

[dependencies]
serde = { workspace = true }
serde_json = "1"
